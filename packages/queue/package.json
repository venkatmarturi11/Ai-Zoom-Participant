{
  "name": "@zoom-assistant/queue",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\" && tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "bullmq": "^5.8.0",
    "ioredis": "^5.4.0",
    "@zoom-assistant/shared": "*",
    "@zoom-assistant/database": "*"
  }
}
