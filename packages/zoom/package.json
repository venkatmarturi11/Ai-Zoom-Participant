{
  "name": "@zoom-assistant/zoom",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\" && tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@zoom-assistant/crypto": "*",
    "@zoom-assistant/database": "*",
    "@zoom-assistant/shared": "*",
    "puppeteer-core": "^22.15.0"
  }
}
