services:
  postgres:
    image: postgres:16-alpine
    container_name: zoom-assistant-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: zoom_assistant
      POSTGRES_USER: zoom_assistant
      POSTGRES_PASSWORD: dev_password
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zoom_assistant"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: zoom-assistant-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
